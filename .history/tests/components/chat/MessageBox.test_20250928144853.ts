import { mount } from '@vue/test-utils';
import MessageBox from '@/components/chat/MessageBox.vue';

describe('Test <MessageBox/>', () => {
  test('Reders input and button properly', () => {
    const wrapper = mount('MessageBox');
  });
});
